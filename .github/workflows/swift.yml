name: Swift

on: [push, pull_request]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - uses: sersoft-gmbh/xcodebuild-action@v1
      with:
        project: App-BDE-iOS.xcodeproj
        scheme: App-BDE-iOS
        destination: platform:iOS Simulator,id:65462205-7FE0-4584-BD84-2008A069D240,OS:14.4,name:iPhone 12
        action: test
